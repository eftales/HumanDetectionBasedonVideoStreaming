Parameter =
{
  // 设备发现阶段参数
  EquipmentDiscovery = {
    Printit = 0;
    NICnum = 0;
    ipaddr_broadcast = "192.168.255.255";
    EQUIPMENTNUM = 2;
    ALLALIVE = 3;
  };



// 人体检测阶段参数
  whichCamera = 1;

// 本设备ID
  ID = 0;


// 摄像头分辨率
  CameraResolution = {
    frame_width = 640;
    frame_height = 480;
  };

// 视频分割参数
  ConfidenceRegion = {
    critical_to_move = 5;     // 可以检测到人的帧数目要达到critical_to_move = 5（默认）才认为有动作
    critical_to_stop = 1;     // 不可以检测到人的帧数目要达到critical_to_move = 1（默认）才认为这是一段有效的图片组
    critical_stoped = 5;      // 可以检测到人的帧数目要达到critical_to_move = 5（默认）才认为有动作真正结束

    // 有效区域:人处在这个区域才算有效
    inner_area = 0.2;

  };


}

